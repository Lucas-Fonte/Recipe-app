import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  ingredientsListContainer: {
    marginTop: 15,
    paddingLeft: 30,
  },
  ingredientContainer: {
    flexDirection: 'row',
    marginBottom: 10,
  },
});

export { styles };
